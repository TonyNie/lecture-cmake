SET(TARGET rslock)
SET(SRC rslock.c)

ADD_LIBRARY(${TARGET} SHARED ${SRC})
INSTALL(TARGETS ${TARGET}
		LIBRARY DESTINATION /usr/local/lib)
ADD_CUSTOM_COMMAND(TARGET ${TARGET}
	POST_BUILD
	COMMAND ${CMAKE_COMMAND} -E copy ${CMAKE_CURRENT_SOURCE_DIR}/rslock.h ${PROJECT_BINARY_DIR}/inc/rslock.h)
