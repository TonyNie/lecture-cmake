CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
INCLUDE(toolchain_mipsel.cmake)
PROJECT(TestCross)

SET(CMAKE_EXE_LINKER_FLAGS '-static')
MESSAGE(STATUS ${CMAKE_SYSTEM_NAME} ${CMAKE_CROSSCOMPILING})
ADD_EXECUTABLE(test main.c)
